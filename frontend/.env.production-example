################################################################################
# FRONTEND - PRODUCTION BUILD ENVIRONMENT
# 
# Build this file using: npm run build
# This is for production deployment (NOT development)
################################################################################

# ══════════════════════════════════════════════════════════════════════════════
# 🔗 API CONFIGURATION - PRODUCTION
# ══════════════════════════════════════════════════════════════════════════════
# In production:
# 1. Vite proxy is NOT available (only in dev server)
# 2. Frontend uses absolute URLs for API calls
# 3. Backend MUST have CORS configured with CLIENT_URL

# Absolute backend URL for production deployment
# Update this to match your actual backend domain
VITE_API_URL=https://api.your-app-domain.com/api

# Fallback backend URL (should be same as VITE_API_URL)
VITE_BACKEND_URL=https://api.your-app-domain.com

# ══════════════════════════════════════════════════════════════════════════════
# ⏱️  TIMEOUT CONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
# Request timeout in milliseconds
# Increase if your MongoDB queries are slow (MongoDB Atlas cold starts can be slow)
VITE_API_TIMEOUT=60000

# Health check timeout
VITE_HEALTH_CHECK_TIMEOUT=10000

# ══════════════════════════════════════════════════════════════════════════════
# 🐛 DEBUG & LOGGING - PRODUCTION
# ══════════════════════════════════════════════════════════════════════════════
# Disable debugging in production for performance
VITE_DEBUG_MODE=false
VITE_DEBUG_NETWORK=false
VITE_DEBUG_API=false

# ══════════════════════════════════════════════════════════════════════════════
# 🚀 DEPLOYMENT NOTES
# ══════════════════════════════════════════════════════════════════════════════
# Building for production:
# 
# 1. Set correct API URLs above (both VITE_API_URL and VITE_BACKEND_URL)
# 
# 2. Build the frontend:
#    npm run build
# 
# 3. Deploy dist/ folder to your static host:
#    - Vercel: git push (auto-builds)
#    - Netlify: drag dist folder
#    - GitHub Pages: npm run build && deploy dist
#    - Docker: ADD dist /app && EXPOSE 80
# 
# 4. Ensure backend is deployed and accessible at VITE_API_URL
# 
# 5. Backend MUST have CLIENT_URL set to your frontend domain:
#    CLIENT_URL=https://your-app-domain.com
