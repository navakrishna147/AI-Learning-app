# ============================================================================
# FRONTEND ENVIRONMENT - EXAMPLE
# ============================================================================
# Copy this file to .env and adjust if needed.

# API base path. In development the Vite proxy forwards /api/* to the backend.
# In production builds set to the absolute backend URL:
#   VITE_API_URL=https://api.example.com/api
VITE_API_URL=/api

# Direct backend URL - used by the Vite proxy target.
# Change this if backend runs on a different host/port.
VITE_BACKEND_URL=http://127.0.0.1:5000

# Request timeout in ms (default 30000 = 30s)
VITE_API_TIMEOUT=30000

# Health check timeout in ms (default 5000 = 5s)
VITE_HEALTH_CHECK_TIMEOUT=5000
