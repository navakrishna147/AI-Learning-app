################################################################################
# FRONTEND - LOCAL DEVELOPMENT ENVIRONMENT
# 
# Copy this file to .env (will be loaded automatically by Vite)
# For LOCAL development with backend on http://localhost:5000
################################################################################

# ══════════════════════════════════════════════════════════════════════════════
# 🔗 API CONFIGURATION - DEVELOPMENT
# ══════════════════════════════════════════════════════════════════════════════
# CRITICAL: Two approaches are available:

# APPROACH 1 (RECOMMENDED): Use Vite Proxy
# - Avoids CORS issues completely
# - Frontend proxy forwards /api/* → http://localhost:5000
# - Backend doesn't need CORS for same-origin requests
# - Works perfectly in development
VITE_API_URL=/api

# APPROACH 2 (FALLBACK): Direct Backend URL
# - Only used if Vite proxy fails
# - Must be accessible and have CORS enabled
# - Use 127.0.0.1 instead of localhost (faster, avoids DNS)
# - Backend .env must have: CORS_ORIGINS=http://localhost:5173
VITE_BACKEND_URL=http://127.0.0.1:5000

# ══════════════════════════════════════════════════════════════════════════════
# ⏱️  TIMEOUT CONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
# Request timeout in milliseconds
# Increase if your MongoDB queries are slow
VITE_API_TIMEOUT=60000

# Health check timeout - how long to wait before considering backend down
# Keep this low for fast failure detection
VITE_HEALTH_CHECK_TIMEOUT=10000

# ══════════════════════════════════════════════════════════════════════════════
# 🐛 DEBUG & LOGGING - DEVELOPMENT
# ══════════════════════════════════════════════════════════════════════════════
# Enable detailed console logging during development
VITE_DEBUG_MODE=true

# Show network error details
VITE_DEBUG_NETWORK=true

# Show API request/response details
VITE_DEBUG_API=true
